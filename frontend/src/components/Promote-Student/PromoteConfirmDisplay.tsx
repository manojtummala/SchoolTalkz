import { Eye, Savelogo, ThumbsUpLogo } from "@/asset/images";
import CommentLogo from "@/asset/images/CommentLogo";
import { SVGProp } from "@/types/ui";
import Input from "@/ui/Input";
import { useState } from 'react';
// import { ValueType } from 'react-select';



interface FromType {
    value: string;
    label: string;
}

interface resultType {
    year: string;
    handle: () => void;
}

// export const Result = ({ year, handle }: resultType) => {
//     const [yearData, setYearData] = useState<ValueType<FromType>>([]);



//     const handleChange = (yearData: ValueType<FromType>) => {
//         setYearData(yearData);

//     }

// }
// export default Result;



