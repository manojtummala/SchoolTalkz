export function sendEmail(email: string, message: string) {
  return { message: "message sent" };
}
export function sendPhoneMessage(phone: string, message: string) {
  return { message: "message sent" };
}
